management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: "*"
        exclude:
          - beans
  endpoint:
    health:
      show-details: always			
  health:
    diskspace:
      enabled: true
server:
  port: 8082
